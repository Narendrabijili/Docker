#!/bin/bash

a=1
b=2

sum=$((a + b))
echo "Sum: $sum"

#  git clone git repo link
#   git status
#      git add docker.sh 
#      git status
#      git commit -m "pusing to git"
#      git status
#      git push -u origin main
